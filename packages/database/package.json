{
  "name": "@oura-pix/database",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply oura-pix-db --local --config ../../api/wrangler.jsonc",
    "db:migrate:prod": "wrangler d1 migrations apply oura-pix-db --remote --config ../../api/wrangler.jsonc",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@oura-pix/types": "workspace:*",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260625.1",
    "@types/node": "^22.9.0",
    "drizzle-kit": "^0.31.10",
    "typescript": "^5.6.3",
    "wrangler": "^4.105.0"
  }
}
