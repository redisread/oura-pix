{
  "name": "@oura-pix/config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'config package has no build step'",
    "lint": "echo 'no lint for config package'"
  }
}
