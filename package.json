{
  "name": "oura-pix",
  "version": "0.2.0",
  "private": true,
  "description": "AI-powered cross-border e-commerce product detail page generator - Monorepo",
  "scripts": {
    "dev": "turbo run dev",
    "dev:api": "turbo run dev --filter=api",
    "dev:web": "turbo run dev --filter=frontend",
    "build": "turbo run build",
    "build:api": "turbo run build --filter=api",
    "build:web": "turbo run build --filter=frontend",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "clean:state": "rm -rf .wrangler/state",
    "db:generate": "pnpm --filter=database db:generate",
    "db:migrate:local": "pnpm --filter=database db:migrate:local",
    "db:migrate:prod": "pnpm --filter=database db:migrate:prod",
    "db:studio": "pnpm --filter=database db:studio",
    "api:dev": "pnpm --filter=api dev",
    "api:deploy": "pnpm --filter=api deploy",
    "web:dev": "pnpm --filter=frontend dev",
    "web:preview": "pnpm --filter=frontend preview",
    "web:deploy": "pnpm --filter=frontend deploy",
    "debug:setup": "echo 'Please copy api/.dev.vars.example to api/.dev.vars and frontend/.env.local.example to frontend/.env.local'",
    "debug:check": "node scripts/check-local-dev.js",
    "debug:full": "pnpm db:migrate:local && turbo run dev --filter=api --filter=frontend"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
